# 数易项目空间

## 查询项目空间列表

调用 MCP 工具 `query_shuyi_projectspaces()` 或通过数易平台查询当前账号有权限的项目空间列表。

## 字段说明

| 字段 | 说明 |
|------|------|
| spaceId | 项目空间ID |
| spaceName | 项目空间名称 |
| spaceCode | 项目空间编码 |
| status | 状态（正常/停用） |

## 约束

- DataDream 和数易**必须同环境**（生产环境 ↔ 生产环境）
- 不同环境的项目空间不可混用